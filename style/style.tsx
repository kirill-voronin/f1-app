import { StyleSheet } from "react-native";

export const textStyle = StyleSheet.create({
  main: {
    fontSize: 16,
    fontWeight: "700",
  },
  header: {
    fontSize: 20,
    fontWeight: "700",
  }
})